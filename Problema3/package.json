{
  "name": "problema3-cucumber",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "npm run cucumber",
    "cucumber": "cucumber-js --require-module ts-node/register --require features/**/*.ts features"
  },
  "devDependencies": {
    "typescript": "^5.1.6",
    "ts-node": "^10.9.1",
    "@cucumber/cucumber": "^9.0.0",
    "@types/node": "^20.0.0"
  }
}
